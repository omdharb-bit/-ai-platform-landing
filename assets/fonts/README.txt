Drop the two required font files from asset_package.zip here:
  display.woff2  -> the display/heading family
  body.woff2     -> the body/text family
Then update the @font-face src + family names in ../styles.css (top of file).
